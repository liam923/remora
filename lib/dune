(menhir
 (modules TexpParser))

(ocamllex TexpLexer)

(library
 (name remora)
 (modules :standard TexpParser TexpLexer)
 (libraries base non_empty_list)
 (preprocess
  (pps ppx_jane ppx_deriving.eq ppx_deriving.show)))

(env
 (dev
  (flags
   (:standard -w -30))))
